"""Two thin example pipelines proving the domain-neutral core: an ML
pipeline (train -> eval) and an app pipeline (build -> test). Everything
domain-specific lives in these files, never in `redthread.adapters.base` or
below.
"""
