"""MCP server: exposes a Redthread store as a coding agent's working
memory, so the same git-backed, portable store is visible on every machine
the agent runs on instead of a folder-local `.claude/`/`.agent/` cache.
"""
